## Ignore Visual Studio temporary files, build results, and
## files generated by popular Visual Studio add-ons.

# -----------------------------
# User-specific files
# -----------------------------
*.rsuser
*.suo
*.user
*.userosscache
*.sln.docstates

# Claude Code agent worktrees (transient, per-session)
.claude/worktrees/

# MonoDevelop / Xamarin
*.userprefs

# Mono crash dumps
mono_crash.*

# -----------------------------
# Build outputs
# -----------------------------
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
# Note: /bld/ in this repo holds shared MSBuild props and helper scripts, NOT build outputs.
# It is intentionally tracked. The legacy `bld/` ignore rule from the .NET template has been
# removed because it silently swallowed pack-codestyle-analyzer.* additions.
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/

# -----------------------------
# Visual Studio
# -----------------------------
.vs/
*/.vs/

Generated\ Files/

# -----------------------------
# Test outputs
# -----------------------------
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*

# NUnit
*.VisualState.xml
TestResult.xml
nunit-*.xml

# BenchmarkDotNet
BenchmarkDotNet.Artifacts/

# Coverlet / coverage
coverage*.json
coverage*.xml
coverage*.info

# Visual Studio coverage
*.coverage
*.coveragexml

# NCrunch
_NCrunch_*
.*crunch*.local.xml
nCrunchTemp_*

# -----------------------------
# .NET / NuGet
# -----------------------------
project.lock.json
project.fragment.lock.json
artifacts/

*.nupkg
*.snupkg

# The in-repo Bodu.CodeStyle.XmlDocumentation analyzer is shipped via a committed
# .nupkg under local-packages/ (restored through NuGet.config's `bodu-local` source)
# so a fresh clone resolves it without an explicit pack step.
!/local-packages/*.nupkg

# Transient solution filter written by tools/Update-CodeStyleAnalyzer.ps1 when it excludes
# benchmark projects; normally deleted at the end of the run, ignored in case a run is interrupted.
_codestyle-refresh.slnf

**/[Pp]ackages/*
!**/[Pp]ackages/build/

# -----------------------------
# Style / analysis
# -----------------------------
StyleCopReport.xml

# -----------------------------
# VS build artifacts
# -----------------------------
*.ilk
*.meta
*.obj
*.pch
*.pdb
*.ipdb
*.pgc
*.pgd
*.rsp
*.tmp
*.log

# -----------------------------
# ReSharper / tools
# -----------------------------
_ReSharper*/
*.[Rr]e[Ss]harper
*.DotSettings.user

*.dotCover

# -----------------------------
# Node / Python
# -----------------------------
node_modules/
__pycache__/
*.pyc

# -----------------------------
# SQL Server
# -----------------------------
*.mdf
*.ldf
*.ndf

# -----------------------------
# MSBuild logs
# -----------------------------
*.binlog

# -----------------------------
# Visual Studio cache files
# -----------------------------

# Generic cache extensions
*.[Cc]ache

# SQLite IDE/workspace caches (safe subset)
slnx.sqlite
*.sqlite-shm
*.sqlite-wal

# Keep .cache directories if explicitly tracked
!?*.[Cc]ache/

# -----------------------------
# DocFX output
# -----------------------------
*/docs/api/**/*.yml
*/docs/api/**/.manifest
*/docs/_site/
*/docs/public/

# -----------------------------
# Local / experimental folders
# -----------------------------
/ConsoleApp2/
/bc-csharp/
**/.cr/personal/

# -----------------------------
# Strong-name signing keys
# -----------------------------
# Commit only the public key (bld/Bodu.public.snk). The PRIVATE key must never be committed —
# supply it from a secret at release time, ideally to a path outside the working tree.
*.snk
!bld/Bodu.public.snk

