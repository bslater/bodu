// ---------------------------------------------------------------------------------------------------------------
// <copyright file="DateOnlyExtensionsTests.FirstDateOfWeekInYear.cs" company="Bodu Pty. Ltd.">
//     Copyright (c) Bodu Pty. Ltd.. All rights reserved.
// </copyright>
// ---------------------------------------------------------------------------------------------------------------

// ---------------------------------------------------------------------------------------------------------------
// <auto-generated />
// ---------------------------------------------------------------------------------------------------------------

using System;

namespace Bodu.Extensions;

public partial class DateOnlyExtensionsTests
{

    /// <summary>
    /// Verifies that <see cref="DateOnlyExtensions.FirstDateOfWeekInYear" /> returns the expected value.
    /// </summary>
    [TestMethod]
    [DynamicData(nameof(DateTimeExtensionsTests.FirstDateOfWeekInYearTestData), typeof(DateTimeExtensionsTests))]
    public void FirstDateOfWeekInYear_ShouldReturnExpectedDay(DateTime inputDateTime, DayOfWeek targetDay, DateTime expectedDateTime)
    {
        var input = DateOnly.FromDateTime(inputDateTime);
        var expected = DateOnly.FromDateTime(expectedDateTime);

        var actual = input.FirstDateOfWeekInYear(targetDay);

        Assert.AreEqual(expected, actual);
    }

    /// <summary>
    /// Verifies that <see cref="DateOnlyExtensions.FirstDateOfWeekInYear" />, when EnumInvalid, throws <see cref="ArgumentOutOfRangeException" />.
    /// </summary>
    [TestMethod]
    public void FirstDateOfWeekInYear_WhenEnumInvalid_ShouldThrowExactly()
    {
        var input = new DateOnly(2024, 1, 1);
        var invalidDay = (DayOfWeek)999;

        Assert.ThrowsExactly<ArgumentOutOfRangeException>(() =>
        {
            _ = input.FirstDateOfWeekInYear(invalidDay);
        });
    }

    /// <summary>
    /// Verifies that <see cref="DateOnlyExtensions.FirstDateOfWeekInYear" />, when MaxYear, returns the expected value.
    /// </summary>
    [TestMethod]
    public void FirstDateOfWeekInYear_WhenMaxYear_ShouldReturnExpected()
    {
        var input = new DateOnly(9999, 12, 31);
        var actual = input.FirstDateOfWeekInYear(DayOfWeek.Saturday); // 9999-01-02 is a Saturday

        Assert.AreEqual(new DateOnly(9999, 1, 2), actual);
    }

    /// <summary>
    /// Verifies that <see cref="DateOnlyExtensions.FirstDateOfWeekInYear" />, when MinValue, returns the expected value.
    /// </summary>
    [TestMethod]
    public void FirstDateOfWeekInYear_WhenMinValue_ShouldReturnValidDate()
    {
        var actual = DateOnly.MinValue.FirstDateOfWeekInYear(DayOfWeek.Monday);

        Assert.AreEqual(new DateOnly(1, 1, 1), actual); // 0001-01-01 is a Monday
    }

}
