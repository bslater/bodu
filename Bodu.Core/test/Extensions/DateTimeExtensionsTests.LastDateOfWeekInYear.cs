// ---------------------------------------------------------------------------------------------------------------
// <copyright file="DateTimeExtensionsTests.LastDateOfWeekInYear.cs" company="PlaceholderCompany">
//     Copyright (c) PlaceholderCompany. All rights reserved.
// </copyright>
// ---------------------------------------------------------------------------------------------------------------

// ---------------------------------------------------------------------------------------------------------------
// <auto-generated />
// ---------------------------------------------------------------------------------------------------------------

using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Bodu.Extensions;

namespace Bodu.Extensions;

public partial class DateTimeExtensionsTests
{
    /// <summary>
    /// Verifies that <see cref="DateTimeExtensions.LastDateOfWeekInYear" />, when Called, returns the expected value.
    /// </summary>
    [TestMethod]
    [DynamicData(nameof(LastDateOfWeekInYearTestData))]
    public void LastDateOfWeekInYear_WhenCalled_ShouldReturnExpectedDate(DateTime input, DayOfWeek dayOfWeek, DateTime expected)
    {
        DateTime actual = input.LastDateOfWeekInYear(dayOfWeek);

        Assert.AreEqual(expected, actual);
    }

    /// <summary>
    /// Verifies that <see cref="DateTimeExtensions.LastDateOfWeekInYear" />, when DayOfWeekIsInvalid, throws <see cref="ArgumentOutOfRangeException" />.
    /// </summary>
    [TestMethod]
    public void LastDateOfWeekInYear_WhenDayOfWeekIsInvalid_ShouldThrowExactly()
    {
        DateTime input = new DateTime(2024, 1, 1);
        var invalidDay = (DayOfWeek)999;

        Assert.ThrowsExactly<ArgumentOutOfRangeException>(() =>
        {
            _ = input.LastDateOfWeekInYear(invalidDay);
        });
    }

    /// <summary>
    /// Verifies that <see cref="DateTimeExtensions.LastDateOfWeekInYear" />, when UsingMinValue, returns <see langword="true" />.
    /// </summary>
    [TestMethod]
    public void LastDateOfWeekInYear_WhenUsingMinValue_ShouldReturnInYear1()
    {
        DateTime input = DateTime.MinValue;
        DateTime actual = input.LastDateOfWeekInYear(DayOfWeek.Monday);

        Assert.IsTrue(actual.Year == 1);
        Assert.IsTrue(actual <= new DateTime(1, 12, 31));
    }

    /// <summary>
    /// Verifies that <see cref="DateTimeExtensions.LastDateOfWeekInYear" />, when UsingMaxValue, returns <see langword="true" />.
    /// </summary>
    [TestMethod]
    public void LastDateOfWeekInYear_WhenUsingMaxValue_ShouldReturnValidDate()
    {
        DateTime input = DateTime.MaxValue;
        DateTime actual = input.LastDateOfWeekInYear(DayOfWeek.Friday);

        Assert.IsTrue(actual <= DateTime.MaxValue);
    }

    /// <summary>
    /// Verifies that <see cref="DateTimeExtensions.LastDateOfWeekInYear" />, when KindIsLocal, returns the expected value.
    /// </summary>
    [TestMethod]
    [DataRow(DateTimeKind.Unspecified)]
    [DataRow(DateTimeKind.Utc)]
    [DataRow(DateTimeKind.Local)]
    public void LastDateOfWeekInYear_WhenKindIsLocal_ShouldPreserveKind(DateTimeKind kind)
    {
        DateTime input = new DateTime(2024, 7, 5, 10, 0, 0, kind);
        DateTime actual = input.LastDateOfWeekInYear(DayOfWeek.Saturday);

        Assert.AreEqual(kind, actual.Kind);
    }

}
