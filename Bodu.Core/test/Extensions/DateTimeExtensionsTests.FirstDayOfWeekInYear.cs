// ---------------------------------------------------------------------------------------------------------------
// <copyright file="DateTimeExtensionsTests.FirstDayOfWeekInYear.cs" company="PlaceholderCompany">
//     Copyright (c) PlaceholderCompany. All rights reserved.
// </copyright>
// ---------------------------------------------------------------------------------------------------------------

﻿// ---------------------------------------------------------------------------------------------------------------
// <auto-generated />
// ---------------------------------------------------------------------------------------------------------------

using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Bodu.Extensions;

namespace Bodu.Extensions
{
    public partial class DateTimeExtensionsTests
    {
        /// <summary>
        /// Verifies that First Day Of Week In Year returns Expected Day.
        /// </summary>
        [TestMethod]
        [DynamicData(nameof(FirstDayOfWeekInYearTestData), DynamicDataSourceType.Method)]
        public void FirstDayOfWeekInYear_ShouldReturnExpectedDay(DateTime input, DayOfWeek targetDay, DateTime expected)
        {
            DateTime actual = input.FirstDayOfWeekInYear(targetDay);

            Assert.AreEqual(expected, actual);
        }

        /// <summary>
        /// Verifies that First Day Of Week In Year preserves Date Time Kind.
        /// </summary>
        [TestMethod]
        public void FirstDayOfWeekInYear_ShouldPreserveDateTimeKind()
        {
            var inputUtc = new DateTime(2024, 4, 1, 0, 0, 0, DateTimeKind.Utc);
            var inputLocal = new DateTime(2024, 4, 1, 0, 0, 0, DateTimeKind.Local);
            var inputUnspecified = new DateTime(2024, 4, 1, 0, 0, 0, DateTimeKind.Unspecified);

            Assert.AreEqual(DateTimeKind.Utc, inputUtc.FirstDayOfWeekInYear(DayOfWeek.Monday).Kind);
            Assert.AreEqual(DateTimeKind.Local, inputLocal.FirstDayOfWeekInYear(DayOfWeek.Monday).Kind);
            Assert.AreEqual(DateTimeKind.Unspecified, inputUnspecified.FirstDayOfWeekInYear(DayOfWeek.Monday).Kind);
        }

        /// <summary>
        /// Verifies that First Day Of Week In Year, when Enum Invalid, throws Exactly.
        /// </summary>
        [TestMethod]
        public void FirstDayOfWeekInYear_WhenEnumInvalid_ShouldThrowExactly()
        {
            var input = new DateTime(2024, 1, 1);
            var invalidDay = (DayOfWeek)999;

            Assert.ThrowsExactly<ArgumentOutOfRangeException>(() =>
            {
                _ = input.FirstDayOfWeekInYear(invalidDay);
            });
        }

        /// <summary>
        /// Verifies that First Day Of Week In Year, when Min Value, returns Valid Date.
        /// </summary>
        [TestMethod]
        public void FirstDayOfWeekInYear_WhenMinValue_ShouldReturnValidDate()
        {
            DateTime actual = DateTime.MinValue.FirstDayOfWeekInYear(DayOfWeek.Monday);

            Assert.AreEqual(new DateTime(1, 1, 1), actual); // 0001-01-01 is a Monday
        }

        /// <summary>
        /// Verifies that First Day Of Week In Year, when Max Year, returns Expected.
        /// </summary>
        [TestMethod]
        public void FirstDayOfWeekInYear_WhenMaxYear_ShouldReturnExpected()
        {
            DateTime input = new DateTime(9999, 12, 31);
            DateTime actual = input.FirstDayOfWeekInYear(DayOfWeek.Saturday); // 9999-01-02 is a Saturday

            Assert.AreEqual(new DateTime(9999, 1, 2), actual);
        }
    }
}