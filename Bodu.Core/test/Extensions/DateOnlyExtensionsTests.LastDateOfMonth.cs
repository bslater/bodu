// ---------------------------------------------------------------------------------------------------------------
// <copyright file="DateOnlyExtensionsTests.LastDateOfMonth.cs" company="PlaceholderCompany">
//     Copyright (c) PlaceholderCompany. All rights reserved.
// </copyright>
// ---------------------------------------------------------------------------------------------------------------

// ---------------------------------------------------------------------------------------------------------------
// <auto-generated />
// ---------------------------------------------------------------------------------------------------------------

using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Bodu.Extensions;

namespace Bodu.Extensions;

public partial class DateOnlyExtensionsTests
{

    /// <summary>
    /// Verifies that <see cref="DateOnlyExtensions.LastDateOfMonth" />, when Called, returns the expected value.
    /// </summary>
    [TestMethod]
    [DynamicData(nameof(DateTimeExtensionsTests.LastDateOfMonthDataTestData), typeof(DateTimeExtensionsTests))]
    public void LastDateOfMonth_WhenCalled_ShouldReturnExpectedDay(DateTime inputDateTime, DateTime expectedDateTime)
    {
        var input = DateOnly.FromDateTime(inputDateTime);
        var expected = DateOnly.FromDateTime(expectedDateTime);
        DateOnly actual = input.LastDateOfMonth();

        Assert.AreEqual(expected, actual);
    }

    /// <summary>
    /// Verifies that <see cref="DateOnlyExtensions.LastDateOfMonth" />, when MinValue, returns the expected value.
    /// </summary>
    [TestMethod]
    public void LastDateOfMonth_WhenMinValue_ShouldReturnExpectedDay()
    {
        DateOnly actual = DateOnly.MinValue.LastDateOfMonth();

        Assert.AreEqual(new DateOnly(1, 1, 31), actual);
    }

    /// <summary>
    /// Verifies that <see cref="DateOnlyExtensions.LastDateOfMonth" />, when MaxValue, returns the expected value.
    /// </summary>
    [TestMethod]
    public void LastDateOfMonth_WhenMaxValue_ShouldReturnExpectedDay()
    {
        DateOnly max = DateOnly.MaxValue;
        DateOnly actual = max.LastDateOfMonth();

        Assert.AreEqual(new DateOnly(9999, 12, 31), actual);
    }
}
