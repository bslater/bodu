# Sample .env file (committed as env.sample; a real deployment copies it to .env).
# Values here are placeholders - no real credentials.
export APP_ENV=production
APP_PORT=8080
DATABASE_URL="postgres://app:placeholder@localhost:5432/app_db"
FEATURE_FLAGS=search,exports # comma-separated, no spaces
GREETING='Hello, operator'
EMPTY_VALUE=
