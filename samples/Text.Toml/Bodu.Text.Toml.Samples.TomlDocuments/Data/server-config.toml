# Server configuration (sample data).
title = "edge-proxy"
workers = 8
drain_timeout = 00:00:30

[tls]
enabled = true
certificate = "certs/edge.pem"

[limits]
max_connections = 10000
max_body_bytes = 1048576
