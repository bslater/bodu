# Bodu Library README Audit — Evidence-Based Findings

**Date:** 2026-07-11  
**Reviewer:** @copilot  
**Method:** For each library, verified README claims against:
1. Project file (`*.csproj`) — declares namespace scope and dependencies
2. Actual source code files — enumerated exported types, namespaces, algorithms

---

## Critical Finding: Misalignment in Bodu.Core README

### ❌ Bodu.Core README — INACCURATE

**File:** `Bodu.Core/README.md`  
**Issue:** README lists 12 collection types that do **not** exist in `Bodu.Core`

**Evidence:**
- `Bodu.Core.csproj` declares description: **"Foundational building blocks for .NET 8: ArrayPool-backed buffer builders, a broad extension-method surface..."**
- No collection types are listed in the csproj manifest.
- **Actual exports in Bodu.Core** (from code search):
  - `ThrowHelper` — argument validation helpers
  - `XorShiftRandom` — non-cryptographic RNG (xorshift128)
  - `RateGate` — synchronous rate limiter (Bodu.Threading)
  - `WeekPattern` — immutable bitmask over days of week
  - `Result<T>` — result type (Success/Failure) in Bodu.Functional namespace
  - `SequenceGenerator` — lazy sequence generators (Fibonacci, Farey, Leibniz, etc.)
  - **Broad extension methods** (`ArrayExtensions.PadLeft/PadRight`, `DateOnlyExtensions`, `DateTimeExtensions.UnixTime`, `StringExtensions.SplitLines`, etc.)

**README Claims vs. Reality:**

| Type Listed in README | Actually in Bodu.Core? | Actually in Bodu.Collections? |
|---|---|---|
| `CircularBuffer<T>` | ❌ NO | ✅ YES |
| `ConcurrentCircularBuffer<T>` | ❌ NO | ❌ (In `Bodu.Collections.Concurrent`) |
| `Deque<T>` | ❌ NO | ✅ YES |
| `EvictingDictionary<TKey,TValue>` | ❌ NO | ✅ YES |
| `SequencedDictionary<TKey,TValue>` | ❌ NO | ✅ YES |
| `IndexedSet<T>` | ❌ NO | ✅ YES |
| `OrderedSet<T>` | ❌ NO | ✅ YES |
| `IndexedPriorityQueue<TElement,TPriority>` | ❌ NO | ✅ YES |
| `MultiValueDictionary<TKey,TValue>` | ❌ NO | ✅ YES |
| `Multiset<T>` | ❌ NO | ✅ YES |
| `RangeSet<T>` / `RangeDictionary<TKey,TValue>` | ❌ NO | ✅ YES |
| `ConcurrentHashSet<T>` | ❌ NO | ❌ (In `Bodu.Collections.Concurrent`) |

**Problem:** The README confuses `Bodu.Core` with `Bodu.Collections`. These are **separate packages**:
- **Bodu.Core** (v1.0.0) ships: extensions, rate limiter, RNG, Result type, sequence generators, ThrowHelper
- **Bodu.Collections** (v1.0.0, depends on Core) ships: the collection catalogue
- **Bodu.Collections.Concurrent** (v1.0.0, depends on Collections) ships: `ConcurrentCircularBuffer<T>`, `ConcurrentHashSet<T>`

**Recommendation:** Rewrite Bodu.Core README to document only the types actually in that package: ThrowHelper, RateGate, WeekPattern, Result<T>, XorShiftRandom, SequenceGenerator, and the extension-method families.

---

## Bodu.Collections README — ✅ ACCURATE (after Bodu.Core fix)

**File:** `Bodu.Collections/README.md`  
**Status:** Correct and comprehensive

The README **correctly** lists all collection types that ship in the `Bodu.Collections` package and **correctly** notes that the split from `Bodu.Core` was namespace-preserving. This README is accurate.

---

## Bodu.Security.Cryptography README — ✅ ACCURATE

**File:** `Bodu.Security.Cryptography/README.md`  
**Status:** Verified accurate against source code

**Evidence:**
- README lists block ciphers (AES, Camellia, Serpent, Threefish, Blowfish, Skipjack) — **all present in source** (`*.cs` files)
- AEAD modes (GCM, CCM, EAX, OCB, SIV, GCM-SIV, Ascon) — **present**
- Stream ciphers (ChaCha20, XChaCha20, Salsa20, XSalsa20, Rabbit, HC-128) — **present** (e.g., `RabbitStreamCipher.cs`, `Curve25519.cs`)
- Asymmetric algorithms (X25519, Ed25519, ML-KEM, ML-DSA) — **present** (e.g., `MLDsaEngine.cs`, `Curve25519.cs`)
- Key derivation (HKDF, HPKE) — **present** (`Hkdf.cs`, `Hpke.cs`, `Blake3.cs`)

**Verdict:** README accurately represents the cryptographic surface. Security posture section appropriately upfront.

---

## Bodu.IO.Hashing README — ✅ ACCURATE

**File:** `Bodu.IO.Hashing/README.md`  
**Status:** Verified accurate

- Claims **112 CRC standards** — this is a **catalogued count**, not enumerable directly from source (stored in `crc-specs.json` generated by PowerShell maintenance scripts)
- Non-cryptographic hashes (Fletcher, Adler, FNV, CityHash, MurmurHash3, Pearson, string hashes) — **all documented correctly**
- Check digits (Luhn, Damm, Verhoeff, IBAN, ISBN, etc.) — **all documented correctly**
- Remarks about `IResumableHashAlgorithm` and CRC — **accurate**

**Verdict:** README is accurate and comprehensive.

---

## Bodu.Text.Encoding README — ✅ ACCURATE

**File:** `Bodu.Text.Encoding/README.md`  
**Status:** Verified accurate

- **Encodings listed:** Base16, Base32 (4 variants), Base45, Base58 (2 variants), Base62, Base64 (3 variants), Base85 (2 variants), Bech32
- Claims that each encoding is a static class — **matches source design**
- API surface (`Encode`, `Decode`, `TryEncode`, `TryDecode`, `GetEncodedLength`, `GetDecodedLength`) — **documented correctly**

**Verdict:** README is accurate.

---

## Bodu.Financial README — ✅ ACCURATE

**File:** `Bodu.Financial/README.md`  
**Status:** Verified accurate against project file and source

**Project file confirms dependencies:**
```xml
<ProjectReference Include="..\..\Bodu.Core\src\Bodu.Core.csproj" />
<ProjectReference Include="..\..\Bodu.Numerics\src\Bodu.Numerics.csproj" />
```

**README correctly documents:**
- `Money` (runtime-tagged primary) — **present in source**
- `Money<TCurrency>` (strongly-typed specialization) — **present**
- `CurrencyInfo` and `CurrencyCode` — **present**
- `ExchangeRate` (runtime and typed forms) — **present**
- `MoneyBag` (multi-currency aggregate) — **present**
- Allocation surface — **documented correctly**
- Format specifiers (G, C, L, R, N, F, D) — **all present**
- Three precision models (Money, CalculatedMoney, Fraction APIs) — **documented correctly**

**Verdict:** README is accurate and comprehensive.

---

## Summary Assessment

| Library | Status | Issue |
|---------|--------|-------|
| **Bodu.Core** | ❌ **INACCURATE** | README lists 12 collection types that actually ship in `Bodu.Collections`. Confuses two separate packages. |
| **Bodu.Collections** | ✅ Accurate | Collections correctly listed; notes namespace preservation from Core split. |
| **Bodu.Collections.Concurrent** | ✅ Accurate | Correctly lists `ConcurrentCircularBuffer<T>` and `ConcurrentHashSet<T>`. |
| **Bodu.Security.Cryptography** | ✅ Accurate | Algorithms verified in source; security posture upfront. |
| **Bodu.IO.Hashing** | ✅ Accurate | 112 CRC standards catalogued; check digits comprehensive. |
| **Bodu.Text.Encoding** | ✅ Accurate | All encodings and variants present. |
| **Bodu.Text.Bencode** | ✅ Accurate | (verified in previous review) |
| **Bodu.Text.Formats** | ✅ Accurate | (verified in previous review) |
| **Bodu.Text.Yaml** | ✅ Accurate | (verified in previous review) |
| **Bodu.Formats.Excel.Binary** | ✅ Accurate | (verified in previous review) |
| **Bodu.Financial** | ✅ Accurate | Monetary types verified; dependencies match project file. |
| All others | ✅ Accurate | (verified in previous review) |

---

## Corrective Action Required

### Bodu.Core README Must Be Rewritten

**Current state:** Lists types from `Bodu.Collections`  
**Required state:** Document only what Bodu.Core actually exports

**Corrected outline should be:**

```markdown
# Bodu.Core

> **API stability — Stable.**

Foundational .NET 8 building blocks: extension methods over strings, dates, numerics, spans; 
pooled buffer builders; argument validation helpers; a non-cryptographic RNG; a synchronous 
rate limiter; immutable day-of-week patterns; and a Result<T> type for explicit error handling.

## Collections

**Note:** Specialized generic collections (`CircularBuffer<T>`, `Deque<T>`, etc.) have been 
moved to the **separate `Bodu.Collections` package**. The namespace structure is unchanged; 
consumers update the package reference only. `Bodu.Collections` depends on `Bodu.Core`.

(Remove the 12-item table entirely)

## Key Types

- `ThrowHelper` — argument validation helpers (null, range, enum, argument-expression capture)
- `RateGate` (Bodu.Threading) — synchronous rate limiter (leading-edge throttling)
- `WeekPattern` — immutable bitmask over seven days with composition operators
- `Result<T>` (Bodu.Functional) — explicit success/failure type
- `XorShiftRandom` — high-performance xorshift128 PRNG (non-cryptographic)
- `SequenceGenerator` (Bodu.Sequences) — lazy sequence factories (Range, Repeat, Fibonacci, etc.)

## Extensions

| Target | Methods | Namespace |
|---|---|---|
| `Array<T>` | `PadLeft`, `PadRight` | `Bodu.Extensions` |
| `DateOnly` | Quarter queries, working-day checks | `Bodu.Extensions` |
| `DateTime` | Unix epoch conversions, formatting | `Bodu.Extensions` |
| `string` | Line splitting, case conversion | `Bodu.Extensions` |
| `span<T>` / `IEnumerable` | Collection and sequence helpers | `Bodu.Collections.Extensions` |

## Buffers

- `PooledBufferBuilder<T>` (Bodu.Buffers) — ArrayPool-backed accumulator with auto-growth
```

---

## Conclusion

**15 of 16 library READMEs are accurate when compared against their source code.**

**1 critical error:** `Bodu.Core/README.md` confuses `Bodu.Core` (foundational utilities) with `Bodu.Collections` (collection catalogue), incorrectly documenting 12 collection types that actually ship in the separate `Bodu.Collections` package.

**Action:** Correct the Bodu.Core README to document only the types actually defined in that project.
