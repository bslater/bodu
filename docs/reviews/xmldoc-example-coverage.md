---
title: XML-doc example coverage
---

# XML-doc `<example>` coverage assessment

This document is the audit trail for the XML-documentation **`<example><code>` uplift** across the
Bodu production libraries. Every public type (and the non-trivial public methods within it) in each
in-scope library is assessed and assigned one of four decisions:

| Decision | Meaning |
|---|---|
| **example added (type)** | A type-level `<example>` showing the headline workflow was added. |
| **example added (method)** | A method-level `<example>` was added to a non-trivial key member. |
| **covered by type** | Member usage is evident from the type-level example; no separate example. |
| **trivial** | Enum, exception, plain DTO/record, `<inheritdoc/>`, or override with no special contract. |

**Scope.** All production libraries; Test/Testing projects, the `Bodu.CodeStyle` solution, generated
files (`*.Designer.cs`, `*.Catalog.cs`, `<auto-generated>`), and embedded-resource/data assets are
excluded.

**Convention.** Examples use the repo-standard block shape:

```
/// <example>
/// <code language="csharp">
///<![CDATA[
/// ... code ...
///]]>
/// </code>
/// </example>
```

---

## Phase 1 — per-library assessment

### Bodu.IO.Compound

| Type / Member | Kind | Decision | Notes |
|---|---|---|---|
| `CompoundStorage` | type | example added (type) | Navigate nested storage; `EnumerateEntries`/`EnumerateStreams`/`TryOpenStorage`/`TryOpenStream`. |
| `SummaryInformation` | type | example added (type) | Reads well-known fields via `CompoundFile.TryGetSummaryInformation`. |
| `DocumentSummaryInformation` | type | example added (type) | Extended metadata + `CustomProperties` enumeration. |
| `OlePropertySet` | type | example added (type) | Lower-level read: `OpenStream` + `Parse` + `TryGetValue` by PID. |
| `CompoundFile` (`Open`/`Create`/`OpenRead`/`IsCompoundFile`) | type | covered by type | Already carried solid examples. |
| `CompoundStream` (`AsMemory`) | type | covered by type | Already carried examples. |
| `OlePropertyValue`, `OlePropertySection`, `CompoundEntryInfo` | DTO | trivial | Value carriers reached through the examples above. |
| `*Builder` authoring types | type | covered by type | Authoring surface, out of read-focused scope; usage transitive. |
| Enums, exceptions, options, comparers, `WellKnownFormatIds` | various | trivial | No non-trivial public method. |

### Bodu.Formats.Excel.Binary

| Type / Member | Kind | Decision | Notes |
|---|---|---|---|
| `ExcelBinaryWorkbook` | type | example added (type) | Open, iterate `Worksheets`, forward-only read loop. |
| `ExcelBinaryWorkbook.OpenRead(string)` / `OpenRead(string, options)` | method | example added (method) | Open by path; numeric-only options. |
| `ExcelBinaryWorkbook.Open(Stream, options)` | method | example added (method) | Stream with `LeaveOpen`. |
| `ExcelBinaryWorkbook.OpenWorksheet(int)` / `ReadWorksheet(int)` | method | example added (method) | Forward-only reader vs. materialized view. |
| `ExcelWorksheetReader` | type | example added (type) | `while (TryReadCell)` switch over `ExcelCellKind`. |
| `ExcelWorksheet` | type | example added (type) | `TryGetCell` + iterate `Rows`/`Cells`. |
| `ExcelBinaryReaderOptions` | type | example added (type) | Configure options, pass to `Open`. |
| `ExcelSerialDate.ToDateTime(double[, ExcelDateSystem])` | method | example added (method) | Serial → DateTime per date system. |
| `ExcelCellReference` (`ToA1`/`TryParseA1`) | type | example added (type) | A1 round-trip. |
| `OpenRead(FileInfo)`, `OpenRead(Stream, bool)`, name-based overloads, `FromSerialDate`, `ColumnName` | method | covered by type | Overload siblings of exampled members. |
| `ExcelCell`, `ExcelRow`, `ExcelWorksheetInfo`, `ExcelWorksheetDimensions`, `ExcelWorkbookProperties` | DTO | trivial | Pure data carriers. |
| Enums, exception types, `Biff8.*` | various | trivial | Enums/exceptions; Biff8 layer is internal. |

### Bodu.Numerics

`Fraction<T>` and `Interval<T>` root types already carried type-level examples; this pass added
method-level examples to the partial member files.

| Type / Member | Kind | Decision | Notes |
|---|---|---|---|
| `Fraction<T>.Parse` / `TryParse` | method | example added (method) | Ratio, mixed, vulgar-glyph; round-trip. |
| `Fraction<T>.ToString(format, provider)` | method | example added (method) | G/M/U/P specifiers. |
| `Fraction<T>` operators | operator | example added (method) | Combined arithmetic + comparison. |
| `Fraction<T>.Add`/`Subtract`/`Multiply`/`Divide`, `Reciprocal`, `Pow` | method | example added (method) | Arithmetic surface. |
| `Fraction<T>` conversions | operator | example added (method) | int/double/decimal both ways. |
| `Fraction<T>.Floor`/`Ceiling`/`Truncate`/`Round` | method | example added (method) | Rounding family. |
| `Fraction<T>.ToContinuedFraction`, `LimitDenominator` | method | example added (method) | Decomposition + round-trip; rational approximation. |
| `Interval<T>.Parse` / `ToString` | method | example added (method) | ISO 31-11 notation. |
| `Interval<T>.IsEmpty` | property | example added (method) | Empty/degenerate/inverted cases. |
| `Fraction<T>`/`Interval<T>` `CompareTo`/`Equals` | method | trivial | Standard BCL contract. |
| `Interval<T>` factories/helpers/set-ops | method | covered by type | Already carried examples. |
| Numerics.Serialization, throw helpers | various | trivial | Out of consumer scope. |

_Bodu.Financial and remaining libraries: rows appended as each pass completes._

---

## Phase 2 — TODO: new in-depth docfx guides & samples

`docs/guides` already has 13 library folders wired through `docs/guides/toc.yml`, per-library
`index.md` card grids, and seven `topics/*.md` landing pages. The following NEW guides would deepen
coverage where the API reference and Phase-1 examples leave gaps. Each is authored as a Markdown
page under the relevant `docs/guides/<library>/` folder and wired into the TOC (see *TOC integration*
below). Ordered by priority.

### P1 — Financial / exchange rates
- `docs/guides/financial/provider-boe.md` — Bank of England feed: cadence, pair coverage, errors.
- `docs/guides/financial/provider-ecb.md` — European Central Bank feed.
- `docs/guides/financial/provider-rba.md` — Reserve Bank of Australia feed.
- `docs/guides/financial/provider-yahoo.md` — Yahoo feed: caveats, symbol mapping.
- `docs/guides/financial/provider-ofx.md` — OFX feed.
- `docs/guides/financial/exchange-rate-aggregation.md` — priority/average strategies, per-pair routing, fallback chains.
- `docs/guides/financial/caching-backends.md` — in-memory vs. TOML-file vs. SQLite vs. distributed/Redis; staleness/refresh.
- `docs/guides/financial/money-allocation-and-rounding.md` — `MoneyBag`, allocation policies, cash rounding edge cases.

### P1 — Excel (binary)
- `docs/guides/excel/formula-cells-and-cached-results.md` — cached formula results, spreadsheet error codes.
- `docs/guides/excel/large-workbook-streaming.md` — forward-only reader, memory bounding.
- `docs/guides/excel/cell-references-and-ranges.md` — A1 parsing, declared used range.

### P1 — IO.Compound
- Deepen `docs/guides/io-compound/property-sets.md` (worked Summary/DocumentSummary extraction).
- `docs/guides/io-compound/office-format-nuances.md` — `.xls`/`.doc`/`.msg` envelope specifics.
- `docs/guides/io-compound/performance-and-large-containers.md` — read strategies, buffering.

### P2 — Numerics
- `docs/guides/numerics/fraction-vs-decimal-vs-double.md` — when to use each; overflow/`BigInteger` promotion.
- `docs/guides/numerics/interval-algebra.md` — worked union/intersection/adjacency.
- `docs/guides/numerics/generic-math-constraints.md` — `IBinaryInteger<T>`/`INumber<T>` constraints in practice.

### P2 — Serialization (Bencode / TOML)
- `docs/guides/serialization/polymorphic-converters.md`.
- `docs/guides/serialization/callbacks-and-events.md`.
- `docs/guides/serialization/error-recovery-and-partial-deserialization.md`.

### P2 — Text formats
- `docs/guides/formats/choosing-a-format.md` — delimited vs. dotenv vs. ini decision matrix.
- `docs/guides/formats/round-tripping-and-mutation.md`.
- `docs/guides/formats/parser-error-recovery.md`.

### P3 — Configuration
- `docs/guides/text-configuration/profile-switching-strategies.md`.
- `docs/guides/extensions-configuration-text/options-pattern-and-reload.md`.
- `docs/guides/extensions-configuration-text/aspnet-core-integration.md`.

### P3 — Cryptography (asymmetric family — the one family with no guide)
- `docs/guides/cryptography/asymmetric-overview.md` (X25519, Ed25519, ML-KEM, ML-DSA).
- `docs/guides/cryptography/key-agreement-x25519.md`, `signatures-ed25519.md`,
  `ml-kem.md`, `ml-dsa.md`.

### P3 — Calendar data bundles
- `docs/guides/calendar/catalogue/region-middle-east.md`, `region-africa.md` (match the existing
  Americas/Europe/Asia-Pacific catalogue pages).

### TOC integration pattern (per new guide)
1. Add the `.md` under `docs/guides/<library>/`.
2. Add a `<div class="bodu-card">` entry to that library's `index.md`.
3. Add the entry under the library's section in `docs/guides/toc.yml`.
4. Surface major guides on the relevant `docs/guides/topics/*.md` landing page.
5. Cross-link the API with `xref:` (e.g. `[xref:Bodu.Financial.ExchangeRate]`; generics use a
   backtick-escaped arity, e.g. `Bodu.Numerics.Fraction\`1`).
