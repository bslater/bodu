<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>Bodu</RootNamespace>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Bodu.Test\test\Bodu.Test.csproj" />
    <ProjectReference Include="..\src\Bodu.Globalization.Recurrence.csproj" />
  </ItemGroup>

  <!-- Public-API snapshot baselines, copied next to the test host for the PublicApiSnapshot gate. -->
  <ItemGroup>
    <None Include="PublicApi\**\*.txt" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <!-- Reconciliation corpora derived from corpus/recurrence/; see that tree's README for provenance. -->
  <ItemGroup>
    <EmbeddedResource Include="Fixtures\Vectors\*.csv" />
  </ItemGroup>

</Project>
